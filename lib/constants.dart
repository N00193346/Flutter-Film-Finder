import 'package:flutter/material.dart';

//This page is used to keep the styling attributes for each page

//Style for search bar
const kSearchBar = InputDecoration(
    filled: true,
    fillColor: Colors.white,
    hintText: 'Search for film',
    hintStyle: TextStyle(
      color: Colors.grey,
    ));
